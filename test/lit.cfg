# -*- Python -*-

import os

config.name = 'medve'
config.suffixes = ['.c', '.cpp']
config.test_format = lit.formats.ShTest()
#config.environment
config.unsupported = False
config.on_clone = None
config.test_exec_root = os.path.dirname(__file__)
config.test_source_root = os.path.dirname(__file__)
config.excludes = []
config.available_features = []
config.target_triple = '-vg'

config.substitutions = []
#config.substitutions.append( ('%clang_cc1', 'clang++ -cc1 -internal-isystem -load %s/sources/libMedve.so -plugin find-explicit-casts' % config.medve_obj_root) )
config.substitutions.append( ('%clang_cc1', 'clang++ -c -Xclang -load -Xclang %s/sources/libMedve.so -Xclang -plugin -Xclang find-explicit-casts' % config.medve_obj_root) )
